<Window x:Class="XProj.Plugin.Translator.TranslatorSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:mi="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        Title="翻译服务设置" Width="570" Height="640" MinWidth="520" MinHeight="540"
        WindowStyle="None" AllowsTransparency="False"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource WindowBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="54" ResizeBorderThickness="7" GlassFrameThickness="0" CornerRadius="18" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{DynamicResource AppBackdropBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="18" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="54" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Margin="22,0,11,0">
                <TextBlock Text="翻译服务设置" VerticalAlignment="Center" />
                <Button HorizontalAlignment="Right" Style="{DynamicResource TitleBarButton}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="Close_Click">
                    <mi:MaterialIcon Kind="Close" Width="14" Height="14" />
                </Button>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="24,6,24,6">
                    <TextBlock Text="密钥仅保存在本机插件数据目录，不会写入项目配置。" Foreground="{DynamicResource MutedTextBrush}" Margin="2,0,0,18" />
                    <TextBlock Text="服务可在翻译主页面顶部随时切换。" Foreground="{DynamicResource MutedTextBrush}" Margin="2,0,0,18" />
                    <Border Style="{DynamicResource InsetCardBorder}" Padding="15" Margin="0,0,0,0">
                        <StackPanel>
                            <TextBlock Text="腾讯翻译需填 SecretId/SecretKey；阿里翻译需填 AccessKey；Google 可选填 API Key。" Foreground="{DynamicResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,0,0,12" />
                            <TextBlock Text="腾讯 SecretId" Style="{DynamicResource FieldLabel}" /><TextBox x:Name="TencentSecretIdBox" Margin="0,0,0,10" />
                            <TextBlock Text="腾讯 SecretKey" Style="{DynamicResource FieldLabel}" /><PasswordBox x:Name="TencentSecretKeyBox" Margin="0,0,0,10" />
                            <TextBlock Text="腾讯地域（可选）" Style="{DynamicResource FieldLabel}" /><TextBox x:Name="TencentRegionBox" Margin="0,0,0,10" />
                            <TextBlock Text="阿里 AccessKey ID" Style="{DynamicResource FieldLabel}" /><TextBox x:Name="AliAccessKeyIdBox" Margin="0,0,0,10" />
                            <TextBlock Text="阿里 AccessKey Secret" Style="{DynamicResource FieldLabel}" /><PasswordBox x:Name="AliAccessKeySecretBox" Margin="0,0,0,10" />
                            <TextBlock Text="阿里服务地址（可选）" Style="{DynamicResource FieldLabel}" /><TextBox x:Name="AliEndpointBox" Margin="0,0,0,10" />
                            <TextBlock Text="Google API Key（可选）" Style="{DynamicResource FieldLabel}" /><PasswordBox x:Name="GoogleApiKeyBox" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,14,24,18">
                <Button Content="取消" Click="Cancel_Click" />
                <Button Content="保存" Style="{DynamicResource PrimaryButton}" Click="Save_Click" Margin="10,0,0,0" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
